# GPT-2 Text Generator with Gradio

An interactive **GPT-2 text generation application** built using **PyTorch**, **Hugging Face Transformers**, and **Gradio**.  
The app automatically detects **CPU or GPU** and runs smoothly on **Google Colab**.

---

## 🚀 Features
- GPT-2 based text generation
- Interactive Gradio web interface
- Adjustable temperature and token length
- Automatic CPU / CUDA detection
- Google Colab compatible with public sharing

---

## 🧰 Technologies Used
- Python
- PyTorch
- Hugging Face Transformers
- Gradio

---

## 📦 Installation
Install required dependencies:
```bash
pip install torch transformers gradio
